# Секрети — ніколи не комітяться
.env
*.bak.*

# Боевая база даних та WAL-файли — не повинні бути в git (реальні дані користувачів)
api/*.db
api/*.db-shm
api/*.db-wal

# Оточення / кеш
venv/
__pycache__/
*.pyc

# Медіа, що генерується під час роботи (не вихідний код)
queue_media/
channel_avatars/
watermarks/
